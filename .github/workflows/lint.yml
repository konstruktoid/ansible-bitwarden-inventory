on: [push, pull_request]
name: Python linting

permissions:
  contents: read

jobs:
  PythonLinting:
    name: Python linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@24cb9080177205b6e8c946b17badbe402adc938f # master
      - name: Konstruktoid Python linting
        uses: konstruktoid/action-pylint@0424e789d078c9b4c4b79baf9c5e40da5071532f # master
